BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Expense] ADD [updatedAt] DATETIME2 NOT NULL CONSTRAINT [Expense_updatedAt_df] DEFAULT CURRENT_TIMESTAMP;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
