BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Expense] ALTER COLUMN [date] DATETIME2 NULL;
ALTER TABLE [dbo].[Expense] ALTER COLUMN [updatedAt] DATETIME2 NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
